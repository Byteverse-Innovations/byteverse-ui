# Local development only - these values are injected at build time from AppsyncStack Secrets Manager
# For local development, get values from Secrets Manager:
# aws secretsmanager get-secret-value --secret-id byteverse-ui/appsync-config --region us-east-1 --query SecretString --output text | jq -r '.VITE_COGNITO_IDENTITY_POOL_ID'

# Required for local development
VITE_COGNITO_IDENTITY_POOL_ID=

# Optional - defaults are used if not set
# VITE_AWS_REGION=us-east-1
# VITE_APPSYNC_ENDPOINT=https://api.byteverseinnov.com/graphql
