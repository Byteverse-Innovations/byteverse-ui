# Local development only
# No environment variables needed - API key is injected server-side via CloudFront Function/Lambda@Edge
# Endpoint can be overridden for local testing if needed

# Optional - defaults are used if not set
# VITE_APPSYNC_ENDPOINT=https://api.byteverseinnov.com/graphql
